<%= form_with(model: acessos_api) do |form| %>
  <% if acessos_api.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acessos_api.errors.count, "error") %> prohibited this acessos_api from being saved:</h2>

      <ul>
        <% acessos_api.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :token %>
    <%= form.text_field :token %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
